
# Environment variables — never commit credentials
.env

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Allure (both regenerated by CI — never commit)
/allure-results/
/allure-report/
